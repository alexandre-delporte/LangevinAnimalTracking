% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{conditional_particle_filter_cpp}
\alias{conditional_particle_filter_cpp}
\title{Conditional particle filter with ancestor sampling (CPF-AS)}
\usage{
conditional_particle_filter_cpp(
  observations,
  sde_params,
  potential_params,
  error_params,
  error_dist,
  polygon_coords,
  U0,
  lambda,
  num_particles,
  scheme,
  split_around_fixed_point,
  proposal_weight,
  reference_trajectory,
  n_sweeps,
  ESS_threshold,
  obs_error_params
)
}
\arguments{
\item{observations}{Matrix with dimensions N x 3: time, Y1, Y2}

\item{sde_params}{List with elements: tau, nu, omega}

\item{potential_params}{List with elements: alpha, B, x_star}

\item{error_params}{List of parameters for the measurement error distribution}

\item{error_dist}{String specifying error distribution: "normal", "scaled_t", or "argos"}

\item{polygon_coords}{Matrix of polygon boundary coordinates (N x 2)}

\item{U0}{Numeric vector of initial state (length 4)}

\item{lambda}{Numeric, penalization parameter}

\item{num_particles}{Integer, number of particles K (K-1 propagated + 1 reference)}

\item{scheme}{String, splitting scheme: "Lie-Trotter" or "Strang"}

\item{split_around_fixed_point}{Logical, whether to split around fixed points}

\item{proposal_weight}{Numeric between 0 and 1, weight for the Gaussian proposal}

\item{reference_trajectory}{Matrix (N x 4) giving the initial conditioning trajectory}

\item{n_sweeps}{Integer, number of CPF-AS sweeps to run (default 1)}

\item{ESS_threshold}{Numeric between 0 and 1, threshold on effective sample size (as a
fraction of num_particles) below which particles 0..K-2 are resampled (systematic
resampling) at a given step. The reference particle's ancestor is always resampled via
ancestor sampling regardless of this threshold. Use 1 to resample at every step.}

\item{obs_error_params}{Optional list of length N of per-observation error_params (ARGOS)}
}
\value{
List with elements:
  \itemize{
    \item trajectory: matrix (N x 4) sampled from the (approximate) smoothing distribution
    \item ess_history: vector of ESS at each time step of the last sweep
    \item resampled_at: logical vector indicating steps where particles 0..K-2 were
      resampled in the last sweep
  }
}
\description{
Implements the CPF-AS kernel (Svensson et al. 2015; Lindsten, Jordan and Schon 2014)
as an MCMC (Particle Gibbs) alternative to forward-filtering backward-sampling for
drawing trajectories from the smoothing distribution. One or more CPF-AS sweeps are
run, each conditioning on the trajectory sampled by the previous sweep (or on
\code{reference_trajectory} for the first sweep), and the final sampled trajectory is
returned. Calling this repeatedly across SGD iterations, each time passing back in the
previously returned trajectory as \code{reference_trajectory}, forms a single persistent
MCMC chain over trajectories (Markovian stochastic approximation).
}
