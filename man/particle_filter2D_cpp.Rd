% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{particle_filter2D_cpp}
\alias{particle_filter2D_cpp}
\title{Run particle filter algorithm for the Langevin diffusion with specified measurement errors
and splitting schemes}
\usage{
particle_filter2D_cpp(
  observations,
  sde_params,
  potential_params,
  error_params,
  error_dist,
  polygon_coords,
  U0,
  lambda,
  num_particles,
  scheme,
  split_around_fixed_point,
  ESS_threshold,
  proposal_weight,
  verbose,
  print_timing
)
}
\arguments{
\item{observations}{Matrix with dimensions N x 3: time, Y1, Y2}

\item{sde_params}{List with elements: tau (persistence), nu (velocity scale), omega (rotation)}

\item{potential_params}{List with elements:
\itemize{
  \item alpha: Numeric vector of mixture weights
  \item B: List of precision matrices
  \item x_star: Matrix of fixed point coordinates
}}

\item{error_params}{List of parameters for the measurement error distribution (contents depend on error_dist):
\itemize{
  \item For "normal": sigma_obs (observation error standard deviation)
  \item For "scaled_t": scale and df (degrees of freedom)
  \item For "argos": sigma_obs, df, rho, a, p
}}

\item{error_dist}{String specifying error distribution: "normal", "scaled_t", or "argos"}

\item{polygon_coords}{Matrix of polygon boundary coordinates (N x 2)}

\item{U0}{Numeric vector of initial state (length 4: position and velocity)}

\item{lambda}{Numeric, penalization parameter (positive number, Inf for no penalization)}

\item{num_particles}{Integer, number of particles to use in the filter}

\item{scheme}{String, splitting scheme: "Lie-Trotter" or "Strang"}

\item{split_around_fixed_point}{Logical, whether to split around fixed points}

\item{ESS_threshold}{Numeric between 0 and 1, threshold on effective sample size for resampling}

\item{proposal_weight}{Numeric between 0 and 1, weight attributed to the previous state in the Gaussian proposal}

\item{verbose}{Logical, whether to print progress messages}

\item{print_timing}{Logical, whether to print profiling timing results (default: FALSE)}
}
\value{
List with elements:
  \itemize{
    \item particles: Array of particles (num_particles x 4 x N)
    \item weights: Matrix of normalized weights (num_particles x N)
    \item total_weights: Vector of sum of unnormalized weights at each time step
    \item loglik: Estimated log-likelihood
    \item loglik_vector: Estimated log-likelihood at each time step
    \item push: Array of inward pushes for each particle at each time step
    \item ancestors: Matrix of ancestor indices
    \item resampled_at: Logical vector indicating steps where resampling was performed
    \item ess_history: Vector of ESS at each time step
    \item timing: List of timing information for code profiling
    \item ind_fixed_point: (if split_around_fixed_point=TRUE) Matrix of fixed point indices
  }
}
\description{
Run particle filter algorithm for the Langevin diffusion with specified measurement errors
and splitting schemes
}
