% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{init_potential_params_hdbscan}
\alias{init_potential_params_hdbscan}
\title{Initialise mixture-of-Gaussians potential parameters from data with HDBSCAN}
\usage{
init_potential_params_hdbscan(
  data,
  coords = c("Y1", "Y2"),
  minPts = NULL,
  alpha_init = 0,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame containing the observed positions}

\item{coords}{Character vector of length 2 giving the names of the
coordinate columns in \code{data} (default \code{c("Y1","Y2")})}

\item{minPts}{Minimum number of points per cluster, passed to
\code{dbscan::hdbscan}. Defaults to 10\% of the number of rows in
\code{data}}

\item{alpha_init}{Initial mixture weight assigned to every cluster found.
Defaults to 0}

\item{verbose}{logical. If TRUE (default), report the number of clusters found}
}
\value{
A list with elements \code{x_star} (J x 2 matrix of cluster
  centroids), \code{B} (list of J precision matrices) and \code{alpha}
  (vector of length J), ready to use as \code{potential_params}
}
\description{
Clusters the observed positions with HDBSCAN (density-based, no need to fix
the number of attraction zones in advance) and turns each non-noise cluster
into one attraction well: the cluster centroid becomes \code{x_star_j} and
the inverse of the cluster's sample covariance becomes the precision matrix
\code{B_j}. The number of wells \code{J} is however many clusters HDBSCAN
finds, not fixed in advance. Same approach as in the bowhead whale and
elephant seal vignettes.
}
