% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llk_gradient.R
\name{llk_gradient_one_step}
\alias{llk_gradient_one_step}
\title{Compute gradient of log-likelihood for one time step
Supports both Lie-Trotter and Strang schemes}
\usage{
llk_gradient_one_step(
  U_next,
  U_prev,
  delta,
  push,
  potential_grad,
  tau,
  nu,
  omega,
  scheme = "Lie-Trotter",
  push_next = NULL,
  potential_grad_next = NULL,
  potential_params = NULL,
  x_star = NULL,
  estimate_centers = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{U_next}{State vector at next time step (X1, X2, V1, V2)}

\item{U_prev}{State vector at previous time step (X1, X2, V1, V2)}

\item{delta}{Time step size}

\item{push}{Push vector at previous time step (length 2)}

\item{potential_grad}{Gradient of potential at previous time step (length 2)}

\item{tau}{RACVM parameter tau}

\item{nu}{RACVM parameter nu}

\item{omega}{RACVM parameter omega}

\item{scheme}{Integration scheme: "Lie-Trotter" or "Strang"}

\item{push_next}{Push vector at next time step (length 2) - REQUIRED for Strang scheme}

\item{potential_grad_next}{Gradient of potential at next time step (length 2)
- REQUIRED for Strang scheme}

\item{potential_params}{Optional list with elements alpha, B for the Gaussian mixture
potential. When provided, gradients w.r.t. potential parameters are also returned.}

\item{x_star}{Optional matrix of attraction centres (one row per component).
Required when potential_params is provided.}

\item{estimate_centers}{Logical. If TRUE (and potential_params/x_star are provided),
gradients w.r.t. the attraction centres x_star are also appended. Default FALSE.}

\item{verbose}{If TRUE, print detailed computation steps}
}
\value{
Named numeric vector with gradients w.r.t. tau, nu, omega, and optionally
  alpha_1,...,alpha_J, logL11_1, L21_1, logL22_1, ..., logL11_J, L21_J, logL22_J,
  and (if estimate_centers) xstar_1_1, xstar_1_2, ..., xstar_J_1, xstar_J_2
  where logL11_k = log(L_k[1,1]) and logL22_k = log(L_k[2,2]) are the
  log-diagonal entries of the lower Cholesky factor of B_k, and xstar_k_l is the
  l-th coordinate of the k-th attraction centre
}
\description{
Compute gradient of log-likelihood for one time step
Supports both Lie-Trotter and Strang schemes
}
